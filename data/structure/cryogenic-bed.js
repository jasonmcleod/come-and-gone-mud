const config = require('../../config');
const range = require('../../lib/range');
const Landmark = require('../../classes/Landmark');

module.exports = (repo) => {
    repo.add({
        name: 'cryogenic bed'
    });
};